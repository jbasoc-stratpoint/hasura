table:
  name: Products
  schema: public
array_relationships:
  - name: CartItems
    using:
      foreign_key_constraint_on:
        column: product_id
        table:
          name: CartItems
          schema: public
