table:
  name: Cart
  schema: public
object_relationships:
  - name: User
    using:
      foreign_key_constraint_on: user_id
array_relationships:
  - name: CartItems
    using:
      foreign_key_constraint_on:
        column: cart_id
        table:
          name: CartItems
          schema: public
