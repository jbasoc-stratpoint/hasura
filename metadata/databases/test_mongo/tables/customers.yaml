table:
  - customers
remote_relationships:
  - definition:
      to_source:
        field_mapping:
          email: email
        relationship_type: array
        source: Postres
        table:
          name: User
          schema: public
    name: customer_details
select_permissions:
  - role: developer
    permission:
      columns:
        - _id
        - username
        - address
        - birthdate
        - email
        - active
      filter: {}
      allow_aggregations: true
      query_root_fields:
        - select_aggregate
        - select
        - select_by_pk
      subscription_root_fields:
        - select_aggregate
        - select
        - select_by_pk
    comment: ""
  - role: guest
    permission:
      columns:
        - _id
        - username
        - address
        - birthdate
        - email
        - active
      filter: {}
      allow_aggregations: true
      query_root_fields:
        - select_aggregate
        - select
        - select_by_pk
      subscription_root_fields:
        - select_aggregate
        - select
        - select_by_pk
    comment: ""
  - role: user
    permission:
      columns:
        - _id
        - username
        - address
        - birthdate
        - email
        - active
      filter: {}
      allow_aggregations: true
      query_root_fields: []
      subscription_root_fields: []
    comment: ""
logical_model: customers
